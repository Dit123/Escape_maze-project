#ifndef ENUMS_H
#define ENUMS_H

// Define game states
enum GameState {
    MENU,
    PLAYING,
    GAME_OVER
};

// Define player directions
enum PlayerDirection {
    UP,
    DOWN,
    LEFT,
    RIGHT
};

#endif

